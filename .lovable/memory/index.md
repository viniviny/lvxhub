Design system: dark theme with amber/orange (#f59e0b) accent. All text in Portuguese (Brazil). Roles stored in user_roles table, never on profiles.

AI content generation uses target market language from activeStore.marketConfig.language (not app UI language). Image prompts always in English. Edge function generate-text handles language/tone mapping per market.
